export const metadata = {
  title: "Stats Léna Moreau",
  alternates: {
    canonical: "/stats/lena",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}