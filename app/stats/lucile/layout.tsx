export const metadata = {
  title: "Stats Lucile Jerome",
  alternates: {
    canonical: "/stats/lucile",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}