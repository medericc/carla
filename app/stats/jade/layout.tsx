export const metadata = {
  title: "Stats Jade Celerier",
  alternates: {
    canonical: "/stats/jade",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}