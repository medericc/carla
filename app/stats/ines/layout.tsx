export const metadata = {
  title: "Stats Ines Debroise",
  alternates: {
    canonical: "/stats/ines",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}